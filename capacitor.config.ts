import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mosharrof.resellerWebsite',
  appName: 'reseller-website',
  webDir: 'dist',
}

export default config
